const initState = {
  userId:"",
  userName:"",
  userEmail:""
}

const userReducer = (state = initState, action: { type: any; payload: any }) => {
  switch (action.type) {
    default:
      return state
  }
}
export default userReducer
