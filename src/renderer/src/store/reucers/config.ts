const initState = {
  version: '1.0.0',
}

const configReducer = (state = initState, action: { type: any; payload: any }) => {
  switch (action.type) {
    default:
      return state
  }
}
export default configReducer
